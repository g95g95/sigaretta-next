# Upstash Redis (REST). Crea un database su https://console.upstash.com e copia i valori.
# Se mancano, l'app usa uno store in memoria (solo per sviluppo locale: non funziona su Vercel serverless).
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
